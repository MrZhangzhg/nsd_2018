tcp时间服务器
1、功能：接受客户机连接、接收客户机发来的消息、把消息加上时间发回客户端
2、流程：
(1) 创建class
(2) class需要绑定哪些属性
(3) class有哪些方法


多进程：
os.fork()有返回值，父进程和子进程各返回一次。子进程返回值是0，父进程的返回值是非
0值(子进程的PID)。

多进程编程思路：
1、明确父子进程都需要做什么操作
2、一般来说，可以让父进程主管生成子进程
3、子进程做具体的工作，做完后退出，否则子进程如果在循环结构中还会再生成子进程











